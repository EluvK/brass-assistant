## 前提：已完成的核对结论（本轮只读验证）

1. **接受你的纠正**：市场补足部分在引擎里**没有选择空间**——`common.rs:147-152` 固定按最便宜档位升序补足，因此"市场价格档位"不需要编码，成本可由"市场/矿计数 + 市场状态"推断。我此前"价格档位丢失"的说法不成立，会在文档中按修正后的口径写。
2. **仍然成立的信息损失**：`common.rs:96-98` 注释明确"player may choose WHICH free sources to drain (**that choice decides whose building flips**)"——免费来源（用谁的矿/铁厂/酒馆）是真实的选择自由度，但 235 维只编码了数量计数；另有 Sell 的 `beer_sources` 被忽略、NetworkDouble 的啤酒具体对象（哪个对手/商家）未编码、Scout 三张弃牌叠加进同一 CARD 块。
3. 其余 docs（architecture / ai-advise / engine-tools / replay-design / ai-python-code-map）核对无漂移；漂移集中在 `ai-tools.md` 与若干代码注释。

## 任务 1：修复文档漂移

**`docs/ai-tools.md`**（契约表）：
- 41 行 `links (6, 39)` → `(7, 39)`；42 行 `global (50,)` → `(114,)`
- 50 行 `ACTION_FEATURE_SCHEMA_VERSION = 2` → `3`
- 133-142 行 checkpoint 字段列表补上实际存在的 `state_feature_schema_version`、`state_feature_shapes`（train.py:190-207）

**代码内过时注释（只改注释/docstring，不改任何逻辑）**：
- `engine/src/bridge/action_features.rs:57` docstring "stable v2 action-feature schema" → v3
- `engine/src/ai/nn_mcts.rs:516-517` 回调注释 `(type (B,7), goal (B,P), value (B,4))` → 实际签名 `(candidate_logits (B,max_N), values (B,4))`
- `python/brass_ai/selfplay.py:43-44` Sample 注释 `(6,39)`/`(50,)` → `(7,39)`/`(114,)`
- `python/brass_ai/rust_mcts.py:79` docstring 引用已不存在的 `brass_ai.mcts.ISMCTS` → 改写
- `python/bootstrap_imitation.py:40` help 文本 "default: 16384" → `131072`
- `engine/src/bridge/encode.rs:250` 测试改名 `v2_encodes_...` → `v3_encodes_...`（仅函数名）

## 任务 2：跑引擎 dump，生成实测示例（临时脚本，不入库）

写临时 Python 脚本（系统 temp）：`be.GameState(seed=7, players=4)` 用 `choose_heuristic()` 推进一整局，逐状态扫 `legal_candidates()`：
- 每类动作（Build/Network/Develop/Sell/Loan/Scout/Pass）取第一个出现的真实候选，列出全部非零位（块名[索引]=值+含义）
- 收集"同一 235 维向量、不同动作"的碰撞实例（尤其 Build 煤来源不同），作为信息损失的实测证据
- 文档中的每个示例都以该实测数据为准

## 任务 3：新建 `docs/ai-action-encoding.md`

1. **总览**：candidate-scoring 链路（`legal_resolved_moves` → `encode_move` 235 维 → `move_codec` canonical → `net.py` 打分 → masked softmax）
2. **235 维布局总表**：11 个块（offset/width/语义）+ SUMMARY 16 偏移逐项含义 + CARD 块语义（含 Scout 叠加行为）
3. **七类动作实测示例**：每个动作一节，"亮位"逐条列出（来自任务 2 的真实数据），直观展示每个操作哪些位亮/暗
4. **价值头与辅助头设计**：value head（4 人终局 VP z-score 向量、MSE、无 tanh、平局全零）；econ head（(income_level, money) 归一化 (x+10)/40、money/100，负收入样本 3× 权重，λ=0.2，canal 样本盖 canal-end 经济、rail 样本盖终局经济）；type 辅助头（7 类，target 由候选 policy 对 `candidates[..., :7]` 边缘化，权重 0.1）；policy CE；L2=1e-4；TrainConfig 默认超参
5. **训练目标来源**：MCTS visit 按 canonical 对齐 / teacher 分数 softmax / full-legal one-hot
6. **uint8 压缩不变量**：所有特征值必须是 0.25 倍数（hierarchical_policy.py:60-70 强制）
7. **已知信息损失（修正口径）**：市场无需编码的原因 + 免费来源身份/Sell beer_sources/NetworkDouble 啤酒对象/Scout 弃牌组合，附实测碰撞证据；迭代方向只引用 `ai-advise.md` 原则，不展开实施计划

## 任务 4：交叉引用与验证

- `docs/ai-tools.md` 契约节加一行指向新文档
- 验证：`python -m pytest python/tests -q`；`cargo test --manifest-path engine/Cargo.toml`（覆盖改名的 encode.rs 测试；Rust 注释改动不需重装扩展，但顺手 `maturin develop --release` 一次确保环境一致）

## 不做的事

- 不改任何逻辑代码；不动 `structural_from_resolved`/`move_codec` 等实现
- 不写 Schema v4 实施计划（后续需要时单独做）