[workspace]
resolver = "3"
members = ["engine"]

# Training data generation is a hot loop of cross-crate calls (serde/bincode,
# engine rules, PyO3 boundary); LTO + one codegen unit buy ~10-20% on those.
[profile.release]
lto = "fat"
codegen-units = 1