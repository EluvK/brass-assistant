[build-system]
requires = ["maturin>=1.7,<2.0"]
build-backend = "maturin"

[project]
name = "brass-engine"
version = "0.1.0"
requires-python = ">=3.9"
dependencies = []

[tool.maturin]
module-name = "brass_engine"
features = ["pyo3/extension-module"]
